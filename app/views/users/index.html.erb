<%= link_to 'Create a new post', user_new_post_path(@user_id) %>

<% @users.each do |user| %>
  <div class="postCont">
    <div class="pic"></div>
       <div class='userCard'>
        <h2><%= user.name %></h2>
        <h3>Number of posts: <%= user.post_counter %></h3>
      </div>
  </div>
<% end %>
